---
phase: 01-local-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - tailwind.config.js
  - postcss.config.js
  - eslint.config.js
  - prettier.config.js
  - index.html
  - .gitignore
  - src/main.tsx
  - src/index.css
  - src/vite-env.d.ts
  - .env.local
autonomous: true

must_haves:
  truths:
    - "npm install completes without errors"
    - "npm run dev starts Vite dev server"
    - "Browser shows React app at localhost:5173"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "@dittolive/ditto"
    - path: "vite.config.ts"
      provides: "Vite configuration with env prefix"
      contains: "envPrefix"
    - path: "src/main.tsx"
      provides: "React entry point"
      contains: "createRoot"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script module import"
      pattern: "src/main.tsx"
---

<objective>
Scaffold React + Vite + TypeScript project with Ditto SDK and Tailwind CSS.

Purpose: Establish the development foundation with all tooling configured before adding Ditto integration logic.
Output: A working React dev environment ready for Ditto SDK initialization.
</objective>

<context>
@/Users/steventurner/projects/ditto-quickstart/pubsec/xcell-print-farm/.planning/PROJECT.md
@/Users/steventurner/projects/ditto-quickstart/pubsec/xcell-print-farm/.planning/ROADMAP.md

Reference implementation (copy patterns from here):
@/Users/steventurner/projects/ditto-quickstart/javascript-web/package.json
@/Users/steventurner/projects/ditto-quickstart/javascript-web/vite.config.ts
@/Users/steventurner/projects/ditto-quickstart/javascript-web/tailwind.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffolding</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    tailwind.config.js
    postcss.config.js
    eslint.config.js
    prettier.config.js
    index.html
    .gitignore
  </files>
  <action>
Create the project structure by copying and adapting from javascript-web reference:

1. Create package.json with:
   - name: "xcell-print-farm"
   - Same dependencies as javascript-web (React 18.3.1, @dittolive/ditto 4.13.1, etc.)
   - Same devDependencies (Vite 6, TypeScript 5.6, Tailwind 3.4, ESLint, Prettier)
   - Same scripts (dev, build, lint, format, preview)

2. Create vite.config.ts:
   - Use @vitejs/plugin-react-swc
   - Set envDir to project root (NOT "../" like reference - this project is standalone)
   - Set envPrefix to "DITTO"

3. Copy these files directly from javascript-web (they work as-is):
   - tsconfig.json (references pattern)
   - tsconfig.app.json
   - tsconfig.node.json
   - tailwind.config.js
   - postcss.config.js
   - eslint.config.js
   - prettier.config.js

4. Create index.html:
   - Title: "xCell Print Farm Monitor"
   - Standard Vite React template structure

5. Create .gitignore:
   - node_modules, dist, .env, .env.local
   - Standard Vite/Node ignores
  </action>
  <verify>
    All files exist in project root:
    ls package.json vite.config.ts tsconfig.json index.html .gitignore
  </verify>
  <done>Project scaffolding complete with all config files</done>
</task>

<task type="auto">
  <name>Task 2: Create React entry point and install dependencies</name>
  <files>
    src/main.tsx
    src/index.css
    src/vite-env.d.ts
    .env.local
  </files>
  <action>
1. Create src/ directory

2. Create src/main.tsx:
   - Standard React 18 createRoot pattern
   - Import index.css
   - Render placeholder App component inline (just a div with "xCell Print Farm Monitor" text)

3. Create src/index.css:
   - Tailwind directives (@tailwind base, components, utilities)

4. Create src/vite-env.d.ts:
   - Reference vite/client types

5. Create .env.local with placeholder values:
   ```
   DITTO_APP_ID=your-app-id
   DITTO_PLAYGROUND_TOKEN=your-token
   DITTO_AUTH_URL=https://your-auth-url
   DITTO_WEBSOCKET_URL=wss://your-websocket-url
   ```

6. Run npm install to install all dependencies

7. Run npm run dev to verify dev server starts
  </action>
  <verify>
    npm run dev starts without errors
    curl -s http://localhost:5173 | grep -q "xCell"
  </verify>
  <done>React app runs in dev mode and displays in browser</done>
</task>

</tasks>

<verification>
1. npm install completes without errors
2. npm run dev starts Vite dev server on port 5173
3. Browser shows "xCell Print Farm Monitor" text
4. No TypeScript or ESLint errors
</verification>

<success_criteria>
- Working React + Vite + TypeScript development environment
- Tailwind CSS configured and ready
- Ditto SDK installed (not yet initialized)
- Environment variables configured for Ditto credentials
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-foundation/01-01-SUMMARY.md`
</output>
