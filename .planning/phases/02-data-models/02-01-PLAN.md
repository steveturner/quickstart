---
phase: 02-data-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Product.kt
  - pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Order.kt
  - pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/OrderLineItem.kt
  - pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/InventoryItem.kt
  - pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/OrderStatus.kt
  - pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Collections.kt
  - pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/ProductModelTest.kt
  - pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/OrderModelTest.kt
  - pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/InventoryModelTest.kt
  - pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/SoftDeleteConventionTest.kt
  - pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/SyncSourceTagTest.kt
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "Product data class has _id, name, category, price (Long cents), imageUrl, deleted, syncSource, lastSyncedAt fields with defaults"
    - "Order data class has _id, items (List<OrderLineItem>), total (Long cents), status (String), timestamp, terminalId, deleted, syncSource, lastSyncedAt with defaults"
    - "InventoryItem data class has _id, productId, quantity (Int), lastModifiedBy, deleted, syncSource, lastSyncedAt with defaults"
    - "All three models have deleted: Boolean = false (soft delete convention)"
    - "All three models have syncSource: String = 'local' default"
    - "OrderStatus enum has OPEN and FULFILLED values"
    - "Collection and field name constants exist for DQL/Firestore references"
  artifacts:
    - path: "pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Product.kt"
      provides: "Product data class"
      contains: "data class Product"
    - path: "pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Order.kt"
      provides: "Order data class with embedded OrderLineItem list"
      contains: "data class Order"
    - path: "pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/InventoryItem.kt"
      provides: "InventoryItem data class"
      contains: "data class InventoryItem"
    - path: "pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/OrderStatus.kt"
      provides: "OrderStatus enum"
      contains: "enum class OrderStatus"
    - path: "pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Collections.kt"
      provides: "Collection and field name constants"
      contains: "object Collections"
  key_links:
    - from: "Order.kt"
      to: "OrderLineItem.kt"
      via: "items: List<OrderLineItem>"
      pattern: "List<OrderLineItem>"
    - from: "Order.kt"
      to: "OrderStatus.kt"
      via: "status stored as String, enum for type safety"
      pattern: "OrderStatus"
---

<objective>
Create Kotlin data classes for all three collections (Product, Order, InventoryItem) plus supporting types (OrderLineItem, OrderStatus) and collection/field name constants. Write unit tests proving field structure, default values, soft-delete convention, and syncSource tagging.

Purpose: Lock the schema contract before any write code (seeder, bridge, repository) exists. Every field name defined here is the single source of truth for both Firestore document fields and Ditto DQL column names.
Output: 6 model/constant files + 5 test files; all tests green.
</objective>

<execution_context>
@/Users/steventurner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steventurner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-models/02-CONTEXT.md
@.planning/phases/02-data-models/02-RESEARCH.md

<interfaces>
<!-- Existing DI structure from Phase 1 — models don't depend on DI directly, but test patterns do. -->

From di/AppModule.kt:
```kotlin
val appModule = module {
    single { /* Ditto instance */ }
    single { /* FirebaseFirestore instance */ }
}
```

From di/RepositoryModule.kt:
```kotlin
val repositoryModule = module {}  // Empty stub — populated in Plan 02
```

Existing test pattern (from Phase 1):
- JVM unit tests in `app/src/test/java/live/ditto/pubsec/pos/`
- Run via `./gradlew :app:test`
- mockk for mocking Android-dependent objects
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create data model classes and constants</name>
  <files>
    pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Product.kt,
    pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/OrderLineItem.kt,
    pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/OrderStatus.kt,
    pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Order.kt,
    pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/InventoryItem.kt,
    pubsec/android-firebase-pos/app/src/main/java/live/ditto/pubsec/pos/data/model/Collections.kt
  </files>
  <behavior>
    - Product has fields: _id (String, default ""), name (String, default ""), category (String, default ""), price (Long, default 0L), imageUrl (String, default ""), deleted (Boolean, default false), syncSource (String, default "local"), lastSyncedAt (Long, default 0L)
    - OrderLineItem has fields: productId (String, default ""), productName (String, default ""), quantity (Int, default 0), unitPrice (Long, default 0L)
    - OrderStatus enum has exactly OPEN and FULFILLED values
    - Order has fields: _id (String, default ""), items (List of OrderLineItem, default emptyList()), total (Long, default 0L), status (String, default "OPEN"), timestamp (Long, default 0L), terminalId (String, default ""), deleted (Boolean, default false), syncSource (String, default "local"), lastSyncedAt (Long, default 0L)
    - InventoryItem has fields: _id (String, default ""), productId (String, default ""), quantity (Int, default 0), lastModifiedBy (String, default ""), deleted (Boolean, default false), syncSource (String, default "local"), lastSyncedAt (Long, default 0L)
    - Collections object defines string constants: PRODUCTS = "products", ORDERS = "orders", INVENTORY = "inventory"
  </behavior>
  <action>
    Create the `data/model/` package directory under `live.ditto.pubsec.pos`.

    **Product.kt**: Data class with all fields having defaults (per CONTEXT.md locked decisions). Price is Long cents. Category is plain String. All defaults satisfy Firestore `toObject<T>()` no-arg constructor requirement.

    **OrderLineItem.kt**: Simple embedded value object. Denormalized product name and unit price for offline display without cross-collection lookups.

    **OrderStatus.kt**: `enum class OrderStatus { OPEN, FULFILLED }`. Used in app code for type safety; serialized to/from String at boundary. No `@Serializable` annotation.

    **Order.kt**: Data class with embedded `List<OrderLineItem>`. Status stored as String (not enum) in the data class for DQL compatibility. Include a companion or extension for `OrderStatus.name` / `enumValueOf()` conversion if useful, but keep the data class field as String per locked decision.

    **InventoryItem.kt**: Data class with quantity as plain Int. The CRDT counter logic (INCREMENT BY) is a Phase 3 repository concern, not a data class concern. `lastModifiedBy` is terminalId for demo visibility.

    **Collections.kt**: `object Collections` with `const val PRODUCTS = "products"`, `const val ORDERS = "orders"`, `const val INVENTORY = "inventory"`. Optionally include field name constants to avoid hardcoded strings in repository/bridge code (e.g., `object Fields { const val ID = "_id"; const val DELETED = "deleted"; const val SYNC_SOURCE = "syncSource"; ... }`).

    All data classes use `val` properties only. No `var`. No platform-specific annotations. Package: `live.ditto.pubsec.pos.data.model`.
  </action>
  <verify>
    <automated>cd pubsec/android-firebase-pos && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 files exist under data/model/, project compiles cleanly, every field matches the schema spec from CONTEXT.md</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for all data models</name>
  <files>
    pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/ProductModelTest.kt,
    pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/OrderModelTest.kt,
    pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/InventoryModelTest.kt,
    pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/SoftDeleteConventionTest.kt,
    pubsec/android-firebase-pos/app/src/test/java/live/ditto/pubsec/pos/SyncSourceTagTest.kt
  </files>
  <behavior>
    - ProductModelTest: default constructor produces valid empty Product; Product with all fields set round-trips via copy(); price is Long type; _id field exists
    - OrderModelTest: default Order has emptyList() items; Order with OrderLineItems computes expected total; status default is "OPEN"; OrderStatus enum has exactly 2 values (OPEN, FULFILLED)
    - InventoryModelTest: default InventoryItem has quantity 0; quantity is Int type; productId references a Product._id shape; lastModifiedBy exists as String
    - SoftDeleteConventionTest: Product, Order, and InventoryItem all default deleted=false; all three classes have a `deleted` Boolean property
    - SyncSourceTagTest: Product, Order, and InventoryItem all default syncSource="local"; all three have syncSource String and lastSyncedAt Long fields; seed data values would use "firestore" (tested in Plan 02)
  </behavior>
  <action>
    Create 5 test files in the existing test package `live.ditto.pubsec.pos`.

    **ProductModelTest.kt**: Test default constructor (all defaults), test with explicit values (price = 499L is $4.99), test that `_id` is String type via assertion, test copy() round-trip.

    **OrderModelTest.kt**: Test default constructor, test with embedded OrderLineItems list (2 items), verify total is Long, verify status defaults to "OPEN", test OrderStatus.values() has exactly 2 entries and names match "OPEN" and "FULFILLED", test that Order.items is `List<OrderLineItem>` type.

    **InventoryModelTest.kt**: Test default constructor (quantity = 0), test with explicit values, verify quantity is Int, verify lastModifiedBy is String.

    **SoftDeleteConventionTest.kt**: For each of Product(), Order(), InventoryItem() — assert `deleted == false`. Use reflection or direct property access to verify the `deleted` property exists and is Boolean type on all three classes.

    **SyncSourceTagTest.kt**: For each of Product(), Order(), InventoryItem() — assert `syncSource == "local"` and `lastSyncedAt == 0L`. Verify valid syncSource values ("firestore", "ditto", "local") can be assigned via copy().

    All tests are pure JVM unit tests — no Android runtime, no mocks needed. These are plain data class instantiations.
  </action>
  <verify>
    <automated>cd pubsec/android-firebase-pos && ./gradlew :app:test 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 test files exist, all tests pass (including pre-existing KoinModuleTest, FirestoreSettingsTest, ExampleUnitTest), ./gradlew :app:test exits 0</done>
</task>

</tasks>

<verification>
- `./gradlew assembleDebug` passes (model files compile)
- `./gradlew :app:test` passes (all model tests + pre-existing tests green)
- Each data class has all fields specified in 02-CONTEXT.md locked decisions
- All fields have default values (Firestore toObject<T>() compatibility)
- No `var` properties, no platform annotations, no @Serializable
- Collections.kt defines string constants for collection names
</verification>

<success_criteria>
- 6 model/constant files exist under data/model/
- 5 test files exist and pass
- Product, Order, InventoryItem all have: _id, deleted, syncSource, lastSyncedAt
- Price/total use Long (cents), not Double
- Order.items is List<OrderLineItem> (embedded, not separate collection)
- OrderStatus enum has OPEN and FULFILLED only
- InventoryItem.quantity is Int (CRDT counter logic deferred to Phase 3 repository)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-models/02-01-SUMMARY.md`
</output>
